import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#000',
        width: '50vh',
    },
    texto: {
        color: '#fff',
    }

})

export default style;